---
description: Follow repo agent conventions from AGENTS.md
alwaysApply: true
---

# Project

- Follow [AGENTS.md](mdc:AGENTS.md) for env, commands, and conventions.
- Prefer existing `backend/`, `frontend/`, `db/`, and `catan_bot/` patterns when adding code.
- Keep changes scoped to what was asked.
- When the user invokes `/concise` or asks to be concise / keep it short / trim it down / cut the fluff / TL;DR (including implicit brevity), read and follow `.agents/skills/concise/SKILL.md` (compose with any other active skill).
- When the user invokes `/push` or attaches the push skill, read and follow `.agents/skills/push/SKILL.md` after the task (or immediately if push is the only request).
- When writing, editing, refactoring, or reviewing Python (including implicit `.py` work, or `/python-coding`), read and follow `.agents/skills/python-coding/SKILL.md`.
- When writing, editing, reviewing, or planning Terraform / AWS infrastructure (including implicit `.tf`/`.hcl` work under `terraform/`, or `/terraform-coding`), read and follow `.agents/skills/terraform-coding/SKILL.md`.
