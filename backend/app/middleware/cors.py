"""CORS configuration. Applied in main via CORSMiddleware."""

# CORS is configured in app/main.py with add_middleware(CORSMiddleware, ...).
# This module can hold CORS-related helpers or allowed-origin logic if needed.
