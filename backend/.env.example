# Required: Postgres connection string from Supabase (Project Settings → Database → Connect).
# Use postgresql:// or postgresql+psycopg2://, not the project HTTPS URL.
# On Render (and other IPv4-only hosts), use the "Session" pooler URL (port 5432), not the direct DB URL.
SUPABASE_DATABASE_URL=postgresql://user:password@db.xxx.supabase.co:5432/postgres

# Required for auth (login/signup): Supabase project URL and anon key.
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# Required for JWT validation (protected routes): Supabase JWT secret
# Get from Supabase: Project Settings → API → JWT Secret
SUPABASE_JWT_SECRET=your-jwt-secret-from-supabase

# Optional: development (default) or production
# ENVIRONMENT=development
